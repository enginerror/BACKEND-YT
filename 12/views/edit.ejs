<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">

      <div class="nav mb-10">
        <a class="text-blue-500" href="/read">Read Users</a>
      </div>

      <h1 class="text-2xl tracking-tighter mb-4">Update User</h1>
      <form action="/update/<%= user.id %>" method="post">
        <input
          class="px-5 py-2 bg-transparent border-2 border-zinc-800 outline-none rounded-sm"
          type="text"
          placeholder="name"
          name="name"
          value="<%= user.name %>"
        />

        <input
          class="px-5 py-2 bg-transparent border-2 border-zinc-800 outline-none rounded-sm"
          type="text"
          placeholder="email"
          name="email"
          value="<%= user.email %>"
        />

        <input
          class="px-5 py-2 bg-transparent border-2 border-zinc-800 outline-none rounded-sm"
          type="text"
          placeholder="image url"
          name="image"
          value="<%= user.image %>"
        />

        <input
          class="px-5 py-2 bg-yellow-500 text-zinc-800 rounded-sm ml-4"
          type="submit"
          value="Update User"
        />
      </form>
    </div>
  </body>
</html>
